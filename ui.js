// For the UI, menu, etc. Anything not directly related to the game.
// Assume we have jquery.
$.ready(function(){
  // Set up the dials click handlers
  $(".ui-dial").

  // Set up menu
});

var OthelloUI = OthelloUI ||
{
  //
  dialHandler: function(e) {
    console.log(e.target);

    //
  }
}
